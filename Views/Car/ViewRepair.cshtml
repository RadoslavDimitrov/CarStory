@model RepairDTO

<table>
    <tr>
        <th>Id</th>
        <th>Description</th>
        <th>CarId</th>
        <th>currCarMilleage</th>
        <th>Status</th>
        <th>CarRepairShopId</th>
    </tr>
    <tr>
        <td>@Model.Id</td>
        <td>@Model.Description</td>
        <td>@Model.CarId</td>
        <td>@Model.currCarMilleage</td>
        <td>@Model.Status</td>
        <td>@Model.CarRepairShopId</td>
    </tr>

    @if (Model.PartsChanged != null)
    {
        <tr><th colspan="6">Parts Changed:</th></tr>
        foreach (var part in Model.PartsChanged)
        {
            <tr>
                <td>@part.Id</td>
                <td>@part.Number</td>
                <td colspan="4">@part.Description</td>
            </tr>
        }
    }
</table>
